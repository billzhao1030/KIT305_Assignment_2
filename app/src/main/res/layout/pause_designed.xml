<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/pausePage"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:id="@+id/goToMenuD"
        android:onClick="complete_game"
        android:layout_width="320dp"
        android:layout_height="80dp"
        android:layout_marginTop="64dp"
        android:text="Go back to menu"
        android:textStyle="bold"
        android:textSize="28sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/continueExerciseD"
        android:layout_width="320dp"
        android:layout_height="80dp"
        android:layout_marginTop="44dp"
        android:layout_marginBottom="64dp"
        android:onClick="designPauseClose"
        android:text="Continue Exercise"
        android:textSize="28sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/goToMenuD" />
</androidx.constraintlayout.widget.ConstraintLayout>